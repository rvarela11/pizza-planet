// @vendors
import React from 'react';

// @styles
import './Header.scss';

const Header = (props) => {
    return (
        <div className="header">
            <figure className="header__logo" />
        </div>
    );
};

export default Header;
